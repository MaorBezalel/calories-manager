/**
 * @fileoverview This file contains the route for the endpoint /.
 *
 * @author Maor Bezalel 325214443
 * @author <firstname> <lastname> <id> @todo add your info Itzik (delete the todo after adding the info)
 */

import { Router } from 'express';

const router = Router();

router.get('/', (req, res) => {
    /**
     * @todo Add the necessary code to handle the GET endpoint / (instructions are below).
     * @todo Create/Use pug files to render the different routes of the app as well as a little description on each route. Mainly to serve as a feedback to users that make requests to the server directly from the browser.
     *
     * @instructions
     *
     *     |
     *     |
     *     V
     *
     * @method GET
     * @description this endpoint is used to render the root view of the app.
     *
     * @params No parameters expected.
     *
     * @functionality The endpoint should render a view that shows a little description of the app and the different routes available.
     */
});

export default router;
